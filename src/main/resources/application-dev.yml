spring:
  datasource:
    url: jdbc:mysql://localhost:3306/searchvidsdev?useSSL=false&serverTimezone=UTC
    username: root
    password: rootpassword
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false

jwt:
  jwtSecret: clientDevSecret
  jwtExpiration: 86400